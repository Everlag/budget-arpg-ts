import { TicksPerSecond } from './ARPGState';
import { 
    RecordFlavor,
    IRecord, IMovementRecord, IDamageRecord, IDeathRecord
} from './Recording';
import * as d3 from 'd3';

console.log(d3.select);

export function visualize(seedState: Object, events: Array<IRecord>) {

}